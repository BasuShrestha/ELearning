@model Elearning.Models.Course;
@{
	Course courseSQLData = ViewBag.SQLData;
	ViewData["Title"] = "CourseView";
}
<h1>@ViewData["Title"]</h1>

<script>
    // Hide alert after 3 seconds
    setTimeout(function () {
        document.getElementById("alert").style.display = "none";
    }, 3000);
</script>

@*   <div class="row">
      <div class="col-md">
          <h3>Course Details</h3>
      </div>
      <div class="col-md-3">
          <a class="btn btn-primary" href="AddCourse">Add Course</a>

      </div>
  </div> *@
<br />

<div class="container mt-4">
    <h2>Fill the Course Details Below</h2>
    <form asp-controller="Course" asp-action="AddCourseDetail" method="post">
        
        <div class="form-group mb-3">
            <label asp-for="Title" class="form-label">Title:</label>
            <input asp-for="Title" class="form-control" required>
        </div>

        <div class="form-group mb-3">
            <label asp-for="Description" class="form-label">Description:</label>
            <input asp-for="Description" class="form-control" required>
        </div>


        <div class="mb-3">
            <button type="submit" class="btn btn-primary">Save Course Details</button>
        </div>
    </form>
</div>

@* 

 @if (courseSQLData.Courses != null && courseSQLData.Courses.Count > 0)
 {
     <div class="container mt-5">
         <h2 class="mb-4">Available Courses</h2>
         <div class="row">
             @foreach (Course course in courseSQLData.Courses)
             {
                 <div class="col-md-4 d-flex align-items-sm-stretch">
                     <div class="card" style="width: 18rem;">
                         <div class="card-body d-flex flex-column">
                             <h5 class="card-title">@course.Name</h5>
                             <p class="card-text">@course.Description</p>
                             <a href="#" class="btn btn-primary mt-auto">Go to course</a>
                         </div>
                     </div>
                 </div>
             }
         </div>
     </div>
 }
 else
 {
     if (courseSQLData.Courses == null)
     {
         <h4>Course is null</h4>
     } 
     else if(courseSQLData.Courses.Count <= 0 )
     {
         <h4>Course count is 0</h4>
     }
     <h3>No data to display...</h3>    
 } *@


  <section class="intro">
      <div class="gradient-custom h-100">
          <div class="mask d-flex align-items-center h-100">
              <div class="container">
                  <div class="row justify-content-center">
                      <div class="col-12">
                          <div id="datatable">
                              <table id="datatable" class="table">
                                  <thead>
                                      <tr>
                                          <th>S.N.</th>
                                          <th>Course Title</th>
                                          <th>Description</th>
                                          <th>actions</th>
                                      </tr>
                                  </thead>
                                  <tbody>
                                      @if (courseSQLData.Courses.Count > 0 && courseSQLData.Courses != null)
                                      {
                                          Console.WriteLine(courseSQLData.Courses.Count);
                                          for (var i = 0; i < @courseSQLData.Courses.Count; i++)
                                          {
                                              if (@courseSQLData.Courses[i].Is_Deleted == 0)
                                              {
                                                  <tr>
                                                      <th scope="row">@courseSQLData.Courses[i].Course_Id</th>
                                                      <td colspan="1">@courseSQLData.Courses[i].Title</td>
                                                      <td colspan="1">@courseSQLData.Courses[i].Description</td>

                                                      <td>
                                                          <div class="btn-group">

                                                               <a class="btn btn-primary" asp-action="UpdateCourse" asp-route-id="@courseSQLData.Courses[i].Course_Id">edit</a>
                                                            @* <form asp-action="UpdateCourse" asp-route-id="@courseSQLData.Courses[i].Id" method="post">
                                                                <button type="submit" class="btn btn-info">Edit</button>
                                                            </form> *@

                                                              <div class="mx-2"></div>

                                                              <form asp-action="DeleteCourse" asp-route-id="@courseSQLData.Courses[i].Course_Id" method="post" onsubmit="return confirm('Are you sure you want to delete this course?');">
                                                                  <button type="submit" class="btn btn-danger">Delete</button>
                                                              </form>
                                                          </div>
                                                      </td>

                                                  </tr>
                                              }
                                          }
                                      }
                                      else
                                      {
                                          <h3>No Courses Found...</h3>
                                      }
                                  </tbody>
                              </table>
                          </div>
                      </div>
                  </div>
              </div>
          </div>
      </div>
  </section>
 