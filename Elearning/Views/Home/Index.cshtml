@model IEnumerable<Elearning.Models.Course>

@{
    ViewData["Title"] = "Home Page";
    var topCourses = ViewBag.TopCourses as List<Elearning.Models.EnrolledCourse>;
    List<Course> courses = ViewBag.AllCourses;
}

@* <div class="container mt-5">
    <form asp-controller="Home" asp-action="Index" method="get" class="row">
        <div class="col-sm-6 col-md-3 mb-3">
            <label for="month" class="form-label">Month:</label>
            <select name="month" id="month" class="form-control">
                @for (int i = 1; i <= 12; i++)
                {
                    @(DateTime.Now.Month == i ? "selected" : "")
                    <option value="@i" >@DateTime.Now.AddMonths(i - DateTime.Now.Month).ToString("MMMM")</option>
                }
            </select>
        </div>
        <div class="col-sm-6 col-md-3 mb-3">
            <label for="year" class="form-label">Year:</label>
            <select name="year" id="year" class="form-control">
                @for (int i = DateTime.Now.Year - 5; i <= DateTime.Now.Year; i++)
                {
                    @(DateTime.Now.Year == i ? "selected" : "")
                    <option value="@i" >@i</option>
                }
            </select>
        </div>
        <div class="col-sm-12 col-md-3 mb-3 d-flex align-items-end">
            <button type="submit" class="btn btn-primary">Filter</button>
        </div>
    </form>
</div> *@

<div class="col-sm-6 col-md-3 mb-3">
    <form asp-controller="Home" asp-action="Index" method="get">
        <div class="form-group">
            <label for="date">Filter by month</label>
            <input type="month" id="date" name="date" class="form-control">
        </div>
        <button type="submit" class="btn btn-primary">Filter</button>
    </form>
</div>

<div class="container mt-5">
    <h2 class="mb-4">Top Enrolled Courses for @{
            @ViewBag.FilterMonth
        } of @{
            @ViewBag.FilterYear
        }</h2>
    @if (topCourses.Count > 0)
    {
        <div class="row">
            @foreach (var c in topCourses)
            {
                <div class="col-md-4 d-flex align-items-stretch">
                    <div class="card" style="width: 18rem;">
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title">@c.Title</h5>
                            <p class="card-text">@c.Description</p>
                            <p class="card-text">Enrollments: @c.EnrolmentCount</p>
                            <a href="#" class="btn btn-primary mt-auto">Go to course</a>
                        </div>
                    </div>
                </div>
            }
        </div>
    } else
    {
        <template>No Courses for this month</template>    
    }
</div>



<div class="container mt-5">
    <h2 class="mb-4">Available Courses</h2>
    <div class="row">
        @foreach (Course c in courses.Where(c => c.Is_Deleted == 0))
        {
            <div class="col-md-4 d-flex align-items-sm-stretch">
                <div class="card" style="width: 18rem;">
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">@c.Title</h5>
                        <p class="card-text">@c.Description</p>
                        <a href="#" class="btn btn-primary mt-auto">Go to course</a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>


@* <div class="text-center">
    <h1 class="display-1">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>
 *@