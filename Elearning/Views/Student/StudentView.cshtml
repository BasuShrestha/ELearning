@model Elearning.Models.Student;

@{
    ViewData["Title"] = "Student Details";
    Student studentSQLdata = ViewBag.sqldata;
}


<script>
    setTimeout(function () {
        document.getElementById("alert").style.display = "none";
    }, 1500);
</script>

@if (ViewBag.Message != null)
{
    <div class="alert alert-info" id="alert" role="alert">
        @ViewBag.Message
    </div>
}

@* <div class="container mt-4">
    <h2>Fill the Student Details Below</h2>
    <form asp-action="AddStudent" asp-asp-controller="Student" method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
       
        <div class="form-group col-md-6">
            <label asp-for="Student_Name" class="control-label">Name</label>
            <input asp-for="Student_Name" class="form-control" />
            <span asp-validation-for="Student_Name" class="text-danger"></span>
        </div>
        <br />
        <div class="form-group col-md-6">
            <label asp-for="Contact" class="control-label">Contact</label>
            <input asp-for="Contact" class="form-control" />
            <span asp-validation-for="Contact" class="text-danger"></span>
        </div>
        <br />
        <div class="form-group col-md-6">
            <label asp-for="Email" class="control-label">Email</label>
            <input asp-for="Email" class="form-control" />
            <span asp-validation-for="Email" class="text-danger"></span>
        </div>
        <br />
        <div class="form-group col-md-6">
            <label asp-for="DOB" class="control-label">Date of Birth</label>
            <input asp-for="DOB" type="date" nameclass="form-control" />
            <span asp-validation-for="DOB" class="text-danger"></span>
        </div>
        <br />
        <div class="form-group col-md-6">
            <label asp-for="Country" class="control-label">Country</label>
            <input asp-for="Country" class="form-control" />
            <span asp-validation-for="Country" class="text-danger"></span>
        </div>
        <br />
        <div class="form-group">
            <input type="submit" value="Insert" class="btn btn-primary" />
            <input type="reset" value="Cancel" class="btn btn-secondary" />
        </div>
    </form>
</div> *@

<a class="btn btn-primary" asp-controller="Student" asp-action="AddStudent">Add Student</a><br />

@if (studentSQLdata.Students.Count > 0)
{
    <br />
<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Student ID</th>
            <th>Student Name</th>
            <th>Contact</th>
            <th>Date of Birth</th>
            <th>Email</th>
            <th>Country</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
            
            @foreach (Student student in studentSQLdata.Students.Where(s => s.Is_Deleted == 0))
            {
                Console.WriteLine(student.Student_Id);
                <tr>
                    <td>@student.Student_Id</td>
                    <td>@student.Student_Name</td>
                    <td>@student.Contact</td>
                    <td>@student.DOB.ToString("yyyy-MM-dd")</td>
                    <td>@student.Email</td>
                    <td>@student.Country</td>
                    <td>
                        @* <a class="btn btn-primary" asp-action="UpdateStudent" asp-route-id="@student.Student_Id">Update</a> *@
                        <form asp-action="UpdateStudent" asp-controller="Student" method="get">
                            <input type="hidden" name="studentID" value="@student.Student_Id" />
                            <input type="submit" value="Update" class="btn btn-primary" />
                        </form>

                        <form asp-action="DeleteStudent" asp-route-id="@student.Student_Id" method="post" onsubmit="return confirm('Are you sure you want to delete this course?');">
                            <button type="submit" class="btn btn-danger">Delete</button>
                        </form>
                    </td>
                </tr>
            }
    </tbody>
</table>
}
else
{
    <h3>No Details of students...</h3>
}